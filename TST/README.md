# MAPS TST Pointer

Reusable Time Space Type logic now lives in the standalone sibling repo:

```txt
../TST
```

Map experiments should import or mirror the `@ric/tst` contract instead of
copying local `name@geohash[@time][#type]` helpers into MAPS.
